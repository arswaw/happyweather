import { BoxesInfoTemplate } from '../../templates/details/boxes-info-template.js'

const BoxesInfo = {
    template: BoxesInfoTemplate,
    data: function() {
        return {
            closed: false
        }
    }
}

export default BoxesInfo